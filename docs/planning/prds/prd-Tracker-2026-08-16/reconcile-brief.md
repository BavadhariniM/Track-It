# Input Reconciliation: docs/brief.md

Checked against `prd.md` and `addendum.md`. 4 gaps found.

- **Moat disclaimer dropped from Vision.** brief.md §"What Makes This Different" (line 32) states explicitly: *"Honestly stated: the moat here is the care and correctness of the rule engine's edge-case handling — not technical novelty. It's an execution advantage, not a defensible one, and should be treated as such."* PRD §1 Vision (lines 18) keeps "Tracker's edge is getting the evaluation logic right... That correctness *is* the product" but drops the explicit disclaimer that this edge is *not* technically novel and *not* a defensible moat. `.memlog.md` line 9 captures "not technical novelty" in the decision log, but the fuller honest-disclaimer framing (execution advantage, not defensible) never made it into the PRD's actual Vision prose or into `addendum.md`. Net effect: the PRD reads as a stronger competitive-moat claim than the brief intended.

- **"Version timeline" UI element missing from PRD scope.** brief.md names this twice as a committed v1 feature: differentiator bullet (line 28) — *"the goal detail view can show its own edit history as a timeline"* — and Scope (line 53) — *"goal detail screen with version timeline, streaks, and stats."* PRD FR-25 (Goal Detail Screen, lines 215-216) lists schedule, target, current/longest Streak, completion percentage, historical calendar, and edit/archive actions, but no version-history/timeline element. FR-3 (Goal Versioning) only covers evaluation semantics, not display. Not mentioned in addendum.md, and no decision to drop it appears in `.memlog.md` — this looks like an unflagged omission rather than a deliberate scope call.

- **Multi-entry-per-day quietly downgraded from a v1 scope commitment.** brief.md Scope (line 48) lists "Boolean and Counter tracking, **including multi-entry-per-day** and negative correction deltas (floored at 0)" under "In for v1" — a direct commitment, not a maybe. addendum.md ("Data Model Notes," lines 16) reclassifies it as architecture that merely "should not be precluded," explicitly noting "MVP UI only exposes a single aggregated daily value" is acceptable — and PRD FR-13 only describes single-value entry/increment, with no FR guaranteeing multiple same-day entries. This is a real scope narrowing versus the brief's explicit "in for v1" language, and unlike the recurrence-scope override, it isn't logged anywhere as a conscious decision.

- **Single-user permanence stated more firmly than brief.md leaves it.** brief.md "Who This Serves" (line 36) deliberately leaves the audience question open: *"Whether this later serves a wider audience beyond personal use is undecided; the design does not currently assume or require it."* PRD §2.2 (line 31) instead asserts Tracker is "explicitly single-user by design, not just for v1... no multi-user data model to grow into" — a firmer, more permanent architectural foreclosure than the brief's stated open question. `.memlog.md` line 10 logs this as a deliberate decision ("single-user forever by design"), so it is a conscious PM call rather than an oversight, but it isn't sourced back to or reconciled against brief.md's explicitly-undecided stance — worth a one-line confirmation that this permanence claim is intended to go beyond what the brief committed to.

Written to: `C:\Users\bavad\OneDrive\Desktop\Track-It\_bmad-output\planning-artifacts\prds\prd-Tracker-2026-08-16\reconcile-brief.md`
